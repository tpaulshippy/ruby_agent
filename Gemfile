# frozen_string_literal: true

source 'https://rubygems.org'

gem 'aws-sdk-core'
gem 'dotenv'
gem 'ruby_llm'
gem 'ruby_llm-mcp'

group :development, :test do
  gem 'debug'
  gem 'rspec'
  gem 'rubocop'
end
